services:
  agent:
    image: powbox-codex:latest
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - codex-config:/home/node/.codex

volumes:
  codex-config:
    name: codex-config
    external: true
