services:
  agent:
    image: powbox-claude:latest
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    volumes:
      - claude-config:/home/node/.claude

volumes:
  claude-config:
    name: claude-config
    external: true
